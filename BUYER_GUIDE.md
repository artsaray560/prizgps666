# 🎯 Bot Deployment & Customization Guide for Buyers

Цей бот повністю налаштовується через один файл конфігурації - `scripts/settings.json`. Ви не потрібно змінювати код!

## ⚡ Quick Start (5 хвилин)

### 1. Скопіюйте конфігурацію
```bash
cp scripts/settings.json.example scripts/settings.json
```

### 2. Отримайте необхідні дані

**Telegram Bot Token:**
- Напишіть @BotFather в Telegram
- Скомандуйте `/newbot`
- Дотримуйтесь інструкцій, отримаєте токен

**Telegram API ID & Hash:**
- Перейдіть на https://my.telegram.org
- Залогіньтесь зі своїм номером телефону
- Перейдіть в "API development tools"
- Створіть Application, отримаєте API ID та API Hash

**Ваш Telegram ID:**
- Напишіть @userinfobot
- Отримаєте своє числовий ID

### 3. Заповніть `scripts/settings.json`
```json
{
  "bot_token": "YOUR_TOKEN_FROM_BOTFATHER",
  "api_id": YOUR_API_ID_FROM_TELEGRAM_ORG,
  "api_hash": "YOUR_API_HASH_FROM_TELEGRAM_ORG",
  "webapp_url": "https://your-webapp-url.com",
  "admin_ids": [YOUR_TELEGRAM_ID],
  "workers": [YOUR_TELEGRAM_ID],
  "target_user": "@your_telegram_username",
  "about_link": "https://t.me/your_channel"
}
```

### 4. Запустіть
```bash
python scripts/main3.py
```

## 📝 Параметри конфігурації

### Обов'язкові
| Параметр | Опис | Приклад |
|----------|------|---------|
| `bot_token` | Токен вашого бота | `7451234567:ABCDEFGHIJKLMNOPQRSTUVWXYZabc` |
| `api_id` | API ID з my.telegram.org | `27696098` |
| `api_hash` | API Hash з my.telegram.org | `abcdef123456789abc` |
| `webapp_url` | URL вашого веб-приложення | `https://my-marketplace.com` |
| `admin_ids` | Список ID адміністраторів | `[123456789]` |

### Важливі
| Параметр | Опис | Приклад |
|----------|------|---------|
| `workers` | ID працівників з доступом до функцій | `[123456789, 111222333]` |
| `target_user` | Цільовий користувач для дропу | `@username` або `123456789` |
| `profit_channel_id` | Канал для логування доходів | `-1001234567890` |
| `logs_channel_id` | Канал для логування помилок | `-1001234567891` |

### Опціональні
| Параметр | Опис | За замовчуванням |
|----------|------|-----------------|
| `control_bot_token` | Токен контрольного бота | "" |
| `maintenance_mode` | Режим обслуговування | `false` |
| `telegram_api_url` | Base URL Telegram | `https://t.me` |
| `about_link` | Ссилка на канал About | `https://t.me/IT_Portal` |
| `nft_fragment_url` | URL для перегляду NFT | `https://t.me/nft` |

## 🌐 Web App Integration

Бот відкриває веб-приложення за URL з конфігурації.

### Локальна розробка
```json
{
  "webapp_url": "http://localhost:3000"
}
```
```bash
npm run dev
```

### Production
```json
{
  "webapp_url": "https://your-domain.com"
}
```

Приложення будуть отримувати `chatId` як параметр URL:
```
https://your-domain.com?chatId=123456789
```

## 🔐 Безпека

### ⚠️ ВАЖЛИВО
- Ніколи не закомітьте `scripts/settings.json`
- Він вже в `.gitignore`
- Для кожного бота використовуйте окремий токен
- Приховуйте `api_hash` та `bot_token`

### За замовчуванням ігнорується:
```
scripts/settings.json  # Ваша конфігурація
scripts/bot.db         # База даних користувачів
scripts/bot.log        # Логи
scripts/*.session      # Pyrogram сесії
```

## 📊 Структура папок

```
rew/
├── scripts/
│   ├── main3.py              # Основний код бота
│   ├── settings.json         # ⚠️ Ваша конфігурація (Git-ігнорирується)
│   ├── settings.json.example # Шаблон конфігурації
│   ├── bot.db                # ⚠️ База даних (Git-ігнорирується)
│   └── ...
├── app/                      # Next.js веб-приложення
│   ├── page.tsx              # Головна сторінка
│   ├── api/                  # API endpoints
│   └── ...
├── lib/
│   └── auth-store.ts         # Зберігання даних користувачів
├── SETUP_GUIDE.md            # Розгорнута інструкція
└── ...
```

## 🚀 Розгортання

### Локально
```bash
# Встановіть залежності
pip install -r requirements.txt

# Запустіть бота
python scripts/main3.py
```

### На сервері
```bash
# Рекомендується використовувати screen або systemd
screen -S telegram_bot python scripts/main3.py

# Або через systemd unit
sudo systemctl start telegram-bot
```

### На Vercel (для веб-приложення)
```bash
# Встановіть Vercel CLI
npm i -g vercel

# Розгорніть
vercel

# Обновіть webapp_url в settings.json
```

## 🛠️ Настройка компонентів

### Додавання нових функцій
1. Додайте параметр в `settings.json`
2. Читайте з `SETTINGS['param_name']` або `SETTINGS.get('param_name', 'default')`
3. Ніякі зміни в коді не потрібні

### Переклад
1. Додайте новий мовний код в текстах (en, ru, etc)
2. Користувачі можуть вибрати мову через `/language`

### Кастомізація кнопок
Всі текстові ссилки беруться з `SETTINGS`:
- `about_link` - ссилка на ваш канал
- `nft_fragment_url` - URL NFT
- `telegram_api_url` - базовий URL

## 📞 Що робити якщо...

### Бот не стартує
```bash
# Перевірьте конфігурацію
python -c "import json; json.load(open('scripts/settings.json'))"

# Перегляньте логи
tail scripts/bot.log
```

### Веб-приложення не відкривається
- Перевірте `webapp_url` в settings.json
- Переконайтесь, що приложення запущено на цій URL
- Проверьте консоль браузера (F12)

### Помилки з API
- Перевірьте `api_id` та `api_hash`
- Переконайтесь, що вони з my.telegram.org
- Спробуйте отримати нові kredytenci

## 📚 Додаткові ресурси

- [Aiogram документація](https://docs.aiogram.dev/)
- [Pyrogram документація](https://docs.pyrogram.org/)
- [Next.js документація](https://nextjs.org/docs)
- [Telegram Bot API](https://core.telegram.org/bots/api)

## 💡 Поради

1. **Тестування локально**: Використовуйте `http://localhost:3000`
2. **Швидке розгортання**: Скопіюйте свій settings.json на сервер
3. **Резервна копія**: Регулярно робіть backup `bot.db`
4. **Логування**: Перевіряйте `bot.log` для помилок
5. **Масштабування**: Додавайте workers через конфігурацію

---

Готово до запуску! Питання? Перевірьте `SETUP_GUIDE.md` для розгорнутої інструкції.
