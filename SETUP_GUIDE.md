# 🚀 Bot Setup & Installation Guide

## Для продавца (ваше текущее окружение)

1. **Установите зависимости**:
```bash
pip install -r requirements.txt
```

2. **Настройте `scripts/settings.json`**:
   - Уже содержит вашу конфигурацию
   - Используется для локальной разработки и тестирования

3. **Запустите бота**:
```bash
python scripts/main3.py
```

---

## Для покупателя (инструкции для клиента)

### ✅ Prerequisites
- Python 3.8+
- pip или conda
- Аккаунт Telegram
- Telegram Bot Token (от @BotFather)
- Telegram API ID и Hash (от my.telegram.org)

### 📥 Установка

1. **Клонируйте или скачайте проект**:
```bash
# Если используется git
git clone <ваш-репо>
cd rew
```

2. **Создайте конфигурацию**:
```bash
# Скопируйте пример конфигурации
cp scripts/settings.json.example scripts/settings.json
```

3. **Отредактируйте `scripts/settings.json`**:
```bash
# Откройте файл в вашем текстовом редакторе
nano scripts/settings.json  # Linux/Mac
# или используйте VS Code, Notepad++ для Windows
```

Обязательно заполните:
- `bot_token` - ваш токен бота
- `api_id` и `api_hash` - от my.telegram.org
- `webapp_url` - URL вашего фронтенда
- `admin_ids` - ваш Telegram ID (получить с помощью @userinfobot)
- `workers` - список ID рабочих
- `target_user` - целевой пользователь для дропа

4. **Установите Python зависимости**:
```bash
pip install -r requirements.txt
```

5. **Запустите бота**:
```bash
python scripts/main3.py
```

### 🔧 Детальная настройка

Подробная документация по всем параметрам конфигурации находится в `scripts/CONFIGURATION.md`.

---

## 🌐 Web App (Next.js Frontend)

### Установка
```bash
# Установите зависимости
pnpm install
# или
npm install
```

### Локальная разработка
```bash
npm run dev
```
Приложение будет доступно на `http://localhost:3000`

### Обновите `scripts/settings.json`
```json
{
  "webapp_url": "http://localhost:3000"
}
```

### Production
```bash
npm run build
npm start
```

---

## 🗄️ Database

Бот использует SQLite для хранения пользователей и подарков:
- Автоматически создается при первом запуске
- Хранится в `scripts/bot.db`
- Не требует отдельной установки

---

## 📊 Первый запуск

1. Запустите бота:
```bash
python scripts/main3.py
```

2. В Telegram найдите вашего бота и нажмите `/start`

3. Вы должны увидеть:
   - Меню на выбранном языке
   - Кнопку "Enter Marketplace"
   - Кнопку "About Platform"

4. При нажатии на "Enter Marketplace" откроется ваше веб-приложение

---

## 🐛 Troubleshooting

### Ошибка: "Bot token is invalid"
- Проверьте правильность `bot_token` в `settings.json`
- Убедитесь, что не добавили пробелы или спецсимволы

### Ошибка: "api_id is invalid"
- Убедитесь, что значение `api_id` - это число
- Получите правильное значение с my.telegram.org

### Бот не отвечает на сообщения
- Проверьте `maintenance_mode` - должен быть `false`
- Убедитесь, что токен активен
- Проверьте логи в консоли

### Web App не открывается
- Проверьте правильность `webapp_url`
- Убедитесь, что фронтенд запущен и доступен по этому URL
- Проверьте консоль браузера на ошибки

---

## 📞 Support

Если что-то не работает:
1. Проверьте `scripts/CONFIGURATION.md`
2. Посмотрите логи в консоли
3. Убедитесь, что все параметры в `settings.json` заполнены правильно

---

## 🔐 Security

⚠️ **ВАЖНО**:
- **Никогда** не коммитьте `scripts/settings.json` в публичный репозиторий
- Добавьте в `.gitignore`:
```
scripts/settings.json
scripts/bot.db
scripts/bot.log
```

- Берегите ваш `bot_token` - это приватный ключ
- Для каждого бота используйте отдельный токен
